"""Agent package.

Each module exposes a single ``run(state)`` function that takes the shared
:class:`workflow.state.GraphState` and returns a partial update dict, in the
style expected by LangGraph nodes.
"""
